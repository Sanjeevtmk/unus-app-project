<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.madproject">

    <uses-sdk
        android:minSdkVersion="9"
        android:targetSdkVersion="21" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MadProject">
        <activity android:name=".book_module"
            android:parentActivityName=".home"></activity>
        <activity android:name=".notes_module"
            android:parentActivityName=".home"/>
        <activity android:name=".questionpaper_module"
            android:parentActivityName=".home"/>
        <activity android:name=".about_pussgrc"
            android:parentActivityName=".home"/>
        <activity android:name=".fees"
            android:parentActivityName=".home"/>
        <activity android:name=".home" />
        <activity android:name=".login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>